import { Administrators } from './administrators';
import { GameParticipant } from './game-participant';

describe('GameParticipant', () => {
  it('should create an instance', () => {
    expect(new Administrators()).toBeTruthy();
  });
});
